package model;

/**
 * The category of a car as classified by the radar's AI recognition model.
 * Different categories are subject to different speed rules.
 */
public enum CarType {
    PRIVATE,
    TRUCK,
    BUS
}